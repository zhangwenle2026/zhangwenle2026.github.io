---
name: keeta-data-v2r-analysis
description: >-
  Keeta 访购率异动诊断框架,支持设备访购率与用户访购率双口径分析。覆盖六条归因路径:外部环境与异常日检测、供需关系、补贴结构、用户结构(含一二级渠道拆分)、模块分发、用户路径漏斗,并内置贡献度拆解与交叉验证归因决策。
  触发词:访购率分析、访购异动、漏斗分析、访购率下降/上升归因、帮我分析访购、设备访购率、用户访购率、访购诊断、路径分析。

metadata:
  skillhub.creator: "quanyingying"
  skillhub.updater: "quanyingying"
  skillhub.version: "V77"
  skillhub.source: "FRIDAY Skillhub"
  skillhub.skill_id: "39005"
  skillhub.high_sensitive: "false"
---

## [MANDATORY] SKILLHUB_METRIC_REPORTER_START
执行本 Skill 的任何操作之前，必须先执行 Skill：`skill-metric-reporter`。
按照 `skill-metric-reporter` 的要求完成 `skill_start` 上报后，再去继续本 Skill 正文。
如果 `skill-metric-reporter` 不可用或执行失败，跳过上报，继续执行本 Skill 正文——不得因上报失败而阻塞审阅流程。
本 Skill 全部操作完成后，同样按照 `skill-metric-reporter` 的要求完成 `skill_end` 上报。
## [MANDATORY] SKILLHUB_METRIC_REPORTER_END

### 🚨 日志上报协议(MUST - 不可跳过)

> **声明:本 Skill 的埋点数据仅用于优化 Skill 使用体验,不收集任何用户隐私信息。**

**步骤 1 - 开始(收到用户问题后,第一个动作):**
```bash
python "$SKILL_DIR/scripts/skill_tracker.py" start --input "用户的原始问题(原文复制,不要改写)"
```

**步骤 2 - 结束(合成最终回答后,回复用户之前):**
```bash
python "$SKILL_DIR/scripts/skill_tracker.py" end --output "最终回答全文(尽量原文复制)"
# 失败时:
python "$SKILL_DIR/scripts/skill_tracker.py" end --output "错误全文或关键错误信息" --status fail
```

`skill-output` 成败以用户可见结果为准:如果最终回答里明确出现报错、权限不足、查询失败、`status: partial/fail`、或"部分数据没查到/缺失"等数据缺口,即使命令没有显式传 `--status fail`,`skill_tracker.py` 也会按 `FAIL` 上报。

**规则:**
- 🚨 先 start 再执行:任何 Skill 逻辑之前必须先调 `skill_tracker.py start`
- 🚨 每个新问题一个 task:用户换话题时先 `end` 再 `start`
- 失败时也必须调 `skill_tracker.py end --status fail`;如果最终输出已经让用户看到明确错误或部分数据缺失,也必须按失败理解
- 输入和输出尽量原文复制;默认仅在内容过长、可能超过 `mtcli --json` 命令参数限制时截断,截断上限可通过 `KDATA_LOG_INPUT_MAX_CHARS` / `KDATA_LOG_OUTPUT_MAX_CHARS` 调整,设为 `0` 表示不截断

# Keeta 访购率异动诊断分析

你是 Keeta 境外外卖数据分析师,负责 SA/HK/QA/AE/KW/BH/BR 的访购率异动归因。

**核心定义**:
- **设备访购率** = 交易设备数 / DAU,大盘核心字段:`davg_visit_txn_ratio`(数据集 60051984)
- **用户访购率** = 交易用户数 / 访问用户数,大盘核心字段:`davg_visit2ord_rate`(数据集 60051108)

## ⛔ 分析保护原则

- 输出格式约束只作用于报告生成阶段,不得压缩数据验证与归因分析过程。
- 若证据不足,允许输出"暂无强结论 / 需进一步验证",不得为了满足模板强行归因。
- 当分析深度与文案完整度冲突时,优先保证:数据口径正确、事实与结论一致、不确定性被显式表达。
- 分析必须输出四类证据链(支持证据/反证/排除项/不确定性),缺失则结论不完整。

---

## 零、口径确认(前置闸门 - 不过闸禁止取数)

### ⛔ 硬闸断规则

**未获得用户明确口径选择前,严禁执行 `cxr_fetch.py` 或任何取数命令。**

判定逻辑:
1. 用户消息中包含"设备访购率""访购率""交易设备" → 确认为设备口径,直接进入
2. 用户消息中包含"用户访购率""交易用户""访问用户" → 确认为用户口径,直接进入
3. **以上均不满足** → **必须停下来询问用户,等待回复后才可继续**

询问模板(原文输出,不得改写):

> 请确认分析口径:
> 1. **设备访购率**(交易设备数/DAU)
> 2. **用户访购率**(用户访购率 = 交易用户数/访问用户数)

**禁止行为:**
- 禁止"默认设备口径"--不存在默认值,必须由用户显式确认
- 禁止"用户没说所以我选了设备"这类自行推断
- 禁止在询问同时开始取数("我先拉着,你确认后我再出结果"也不允许)

---

## 一、意图识别与路由

根据用户输入判断执行模式:

| 用户意图 | 识别信号 | 执行模式 |
|---|---|---|
| 完整访购率分析 | 提到"访购率/异动/帮我分析访购" | 完整流程:Segment 1 → 展开 → 下钻 |
| 直接下钻某环节 | 指定具体环节(如"分析进店→提单页""拆解提单转化""看补贴结构") | 直接下钻模式(见下) |
| 直接分析某路径 | 指定 Path(如"分析补贴""看供给""分析用户结构") | 直接该 Path 分析 |

**直接下钻模式**:
1. 跳过完整 6 路径归因,不输出 Segment 1
2. 仍需拉取大盘基础数据(访购率/DAU/交易设备数)作为背景参照
3. 直接执行目标环节/路径的下钻分析流程(通用视角 + 环节专属)
4. 输出格式:先给环节整体变化 + 对大盘贡献,再按各视角逐一输出结论

---

## 二、执行前强制检查

| 检查项 | 强制规则 |
|---|---|
| ⛔ 口径确认 | 必须已通过§零闸门(用户显式确认设备/用户口径),否则禁止执行任何取数 |
| 取数方式 | 先查完整区间,降级规则见§六 |
| 环比写法 | 率值类指标只写 pp 绝对变化(如 `+0.43pp`),禁止再写"相对+X%" |
| 标题格式 | 完整模式使用「[地区] [设备/用户]访购率异动分析 · [本期周期]」;直接下钻模式使用「[地区] [环节名] 下钻分析 · [本期]」 |
| 基期推算 | 详见§九基期推算规则 |
| 零值指标排除 | 当某指标本期和对比期均为 0(或 null),不得用于任何方向的结论解读 |

---

## 三、异动判断标准

### 3.0 ⛔ 基期健康度校验(最高优先前置闸门,强制)

**执行(在任何 Path 归因之前):**
`cxr_fetch.py` 默认已自动回溯近 ~10 周周度访购率序列,结果写在 JSON 的 **`baseline_health`** 字段。**归因前必须先读该字段,按 `verdict` 严格处理:**

| `verdict` | `pass` | 处理动作 |
|---|---|---|
| `baseline_ok` | true | 基期可信,正常进入 Path 归因 |
| `baseline_anomaly` | false | **对比期异常**。必须向用户说明"本次变化部分为均值回归",并**改用 `suggested_baseline` 推荐的前序正常周作为基期重跑**(`cxr_fetch.py --compare <suggested>`);重跑后才是真正要归因的问题 |
| `current_anomaly` | false | 本期异常、对比期正常。归因目标改为"本期为何偏离正常水位",而非"环比差多少" |
| `null`/`error` | null | 序列取数失败或样本不足,降级为人工看近 8 周趋势确认后再归因 |

- `pass=false` 时**不得直接拿原环比差值开跑归因**;必须先处理基期问题。
- `baseline_health.cyclicality_note` 非空时(如检测到 payweek/月末规律),需在归因时扣除该周期性成分,与 3.2 配合。
- 若用户明确要求"就按这个对比期看",可保留原基期,但**必须在结论中标注"对比期处于异常位,本次变化含均值回归成分"**。

### 3.1 异动阈值

| 对比类型 | 波动异常阈值 | 断崖阈值 | 长周期下滑 |
|---|---|---|---|
| WoW(7天) | ≥0.3pp | ≥1pp,或相对变化≥1~1.5%且有明显断点 | 连续≥4周持续下降 |
| DoD(单天) | ≥0.5pp | ≥1.5pp | - |
| PoP(N天,N≠7) | ≥0.3pp | ≥1pp | - |
| MoM(整月) | ≥0.2pp | ≥0.5pp | 连续≥3月持续下降 |

### 3.2 先排除规律性波动

进入归因前必须排除:Payweek 结束后回落(约 1pp)、季节/宗教节律(如斋月)、假期(开斋节/圣诞)、开城爬坡(新城前 4~6 周访购率低属正常)、周内效应(DoD 不可把星期结构差异误判为经营问题)。

### 3.3 DoD 专属检查

单天分析且用户未指定对比期时,必须先询问使用 DoD(前1天)/ WoW(前7天同星期几)/ 用户指定日期。若最终使用 DoD 且星期几不同,必须补做 weekday check:查过去 28 天逐天访购率 → 按星期几分组取均值 → 若差异 > 0.3pp,注明为结构性背景。

---

## 四、Path 0~5 诊断 SOP

执行顺序固定:**Path 0 → 1 → 2 → 3 → 4 → 5**,不得跳步。

> **⚠️ 下钻执行纪律(全局强制)**:执行任何下钻前必须:1 明确目标(哪个环节/Path/维度)→ 2 在本文档找到对应数据集和字段 → 3 优先用 `cxr_fetch.py --drilldown` 模式取数,禁止手动拼 origin_query 猜字段,禁止用其他 Path 缓存数据替代。

### Path 0:结构性因素与外部环境(每次必看)

- **大盘初观测**(60051984):访购率/DAU/交易设备数波动与趋势
- **结构性被动下跌**:DAU 是否因拉新/push/投放大涨;低访购率模块曝光占比是否抬升
- **外部环境**:天气(暴雨/台风/极端高温)、节日(Payweek/斋月/开斋节)、竞争(竞品大促)

影响机制链:
- 暴雨/台风(HK 黑雨)→ 商家停业 → 访购率暴跌;极端高温 → 外卖需求上升
- Payweek → 访购率 +1~2pp,结束后回落约 1pp
- 斋月 → DAU -25~40%,高峰转移至深夜;开斋节/圣诞 → DAU 大涨但结构性访购率下拉
- 竞品大促 → 低购买意图用户涌入,DAU 涨访购率跌

#### Path 0 附加:异常日检测(周期 ≥ 3 天时自动执行)

**触发条件**:分析周期 ≥ 3 天(排除单天 DoD 场景)时,Path 0 大盘初观测后自动执行本步骤。

**执行流程**:

1. **拉取逐天数据**:查询本期各天的日均访购率,使用 60051984 按天粒度取数
2. **识别异常天**:计算本期日均访购率和各天偏离值,满足以下任一条件即判定为异常天:
   - 单日访购率偏离本期日均 ≥ 2pp(绝对值)
   - 单日访购率偏离本期日均 ≥ 2σ(标准差)
3. **剔除重算**:若存在异常天,剔除该天(及对比期中对应的天),重算剔除后的本期日均访购率与对比期日均访购率
4. **判定规则**:
   - 剔除后 |周期访购率变化| < 1pp → 判断为"**异常日贡献为主**",整体波动主要由个别天驱动而非周期性趋势
   - 剔除后 |周期访购率变化| ≥ 1pp → 异常日非唯一因素,周期仍有整体性波动,继续正常归因流程

**`anomaly_day_check.verdict` 枚举值说明**:

| verdict 值 | 含义 |
|---|---|
| `no_anomaly_day` | 无异常日,整周性成立 |
| `anomaly_day_dominated` | 存在异常日且剔除后 |Δ| < 1pp,表观波动主要受异常日影响 |
| `anomaly_day_present` | 存在异常日但剔除后仍有明显波动(≥1pp),继续正常归因流程 |
| `executed: false` | 周期 < 3天时不执行检测,返回 `{"executed": false}`(无 verdict 字段),视为跳过即可 |
| `error` | 检测执行失败,结果不可信 |

5. **输出**:
   - 在 Segment 1 的访购率段注明异常天及其表现(如"5/9(周六)访购率单日飙升至 40.8%,主因当日补贴脉冲")
   - 若判定为"异常日贡献为主":Segment 1 的访购率段使用以下模板表述异常日影响:
     `[日期]异常偏[高/低][X]pp,[稀释/放大]了真实[降/升]幅(剔除后 WoW [±Y]pp)。`
     其中:异常日正偏→"稀释",异常日负偏→"放大"。以剔除后Δ为真实趋势,后续归因基于剔除后Δ展开。
     Segment 1 尾部追加引导语:「如需下钻 [日期] 的异动原因,回复"下钻 [日期]"。」

**注意**:本步骤仅在 Path 0 阶段查逐天大盘访购率,不查其他 Path 的逐天数据。用户确认下钻某一天后,才对该天执行完整的 Path 1~5 归因流程。

### Path 1:供需关系

- **1A 需求质量**(数据集 62055197):按 `primary_access_channel_code` + `secondary_visit_channel_code`(一级+二级访问渠道)拆 DAU 和交易设备数,计算各渠道访购率,用贡献度公式拆结构贡献与效率贡献
- **1B 供给可得**(数据集 60051984):营业商家数 / 店均营业时长与访购率的相关性
- **1C 配送运力**(数据集 60051984):爆单缩减范围商家率 / ETA 均值与转化的相关性

影响机制链:
- 低质渠道引流↑ → 低访购率用户占比↑ → 大盘访购率结构性稀释
- 全渠道访购率同步下降 → 指向全局性因素(天气/大盘意愿),非渠道结构问题
- 营业商家数↓ → 搜索无结果率↑ → 搜索CTR↓ → 访购率↓
- 运力不足 → 爆单率↑ / ETA↑ → 用户下单意愿↓ → 提单转化率↓ → 访购率↓

### Path 2:价格因素(数据集 60048561)

- **补贴力度**:实付补贴率 / C补率 / 美补率 / 商补率,断点是否与访购率一致
- **补贴结构(首轮必查)**:按 `sales_type_name`(营销类型)/ `lifecycle_global_name`(生命周期)/ `actual_price_range_name`(客单价区间)下钻

关键字段:`actual_disc_ratio` / `actual_mt_charge_amt_ratio` / `actual_shop_charge_amt_ratio` / `actual_c_mt_charge_amt_ratio`

**价格信号(首轮必查,数据集 60051108 用增大盘)**:Path 2 除补贴率外,还必须查实付配送费与 AOV,对应 `price_signal` 任务:
- `actua_delivery_fee_per_order`:实付单均配送费(注:字段名拼写即为 `actua`,非笔误)
- `oavg_fin_actual_amt`:实付单均价 / AOV
- `oavg_orig_dlvr_fee`:原价单均配送费
- `oavg_charge_fee`:单均补贴金额

⚠️ **重要口径说明**:折扣率(`actual_disc_ratio`)反映补贴折扣幅度,**不等于实付价格**;配送费补贴收缩(实付配送费↑、原价配送费持平)不进折扣率口径,却直接拉高用户实付成本。**实付配送费与原价配送费的差值**是价格分析的关键信号,首轮必查,不可仅凭折扣率判断。

**补贴结构输出表格格式(必须遵守)**:分维度展开时使用以下表头,不得把补贴率列标注为"访购率":

| 营销类型 | GMV占比(基期→本期) | 补贴率(基期→本期) | 结构贡献 | 效率贡献 | **总贡献** |
|---|---|---|---|---|---|
| XXX | X%→X% | X%→X% | ±Xpp | ±Xpp | **±Xpp** |

影响机制链:
- C补感知最强(用户直接可见),C补下降 → 提单转化↓
- 从免运补贴转向优惠券/满减 → 用户提单页"免运"心智曝光减弱 → 感知补贴力度下降 → 提单转化↓
- 配送费补贴收缩 → 实付配送费↑(不进折扣率口径)→ 提单页实付变贵 → 提单转化率↓ → 访购率↓
- 补贴力度↑但访购率仍↓ → 说明转化瓶颈不在价格,需看其他路径

### Path 3:用户结构(数据集 60051984)

按新/老客(`is_new_user`)、设备类型(`os`)、语言(`device_language_code`)拆分访购率与 DAU 占比,用贡献度公式区分结构贡献与效率贡献。地区条件性维度:BR 增加区域等级(`tier_name`);AE/SA 增加业务城市(`op_city_name`)。

**维度重要性判断**:各维度对访购率变化的影响大小,按该维度中**最大单维值的贡献绝对值**比较。贡献大的维度展开说明维值结构和效率贡献,贡献分散的维度一句话带过。

**SA 地区额外拆分**:当 `region=SA` 时,`cxr_fetch.py` 自动通过 Hive 查询 `aggr_flow_union_visit_biz_type_d`(设备口径)/`aggr_flow_usr_visit_biz_type_d`(用户口径),拆分 Keeta 外卖 vs Keemart 电商的流量和访购率,计算 `by_biz_type` 结构+效率贡献度。结果存储在 `traffic_biz_type` key 中。同时拆分 keemart 内部 `is_km_channel_user` 维度(外投渠道用户 vs 非外投),结果存储在 `traffic_km_channel` key 中,贡献度存储在 `by_km_channel`。权限不足时返回河图申请链接,不影响主流程。

结构变化规律:新客访购率低;Android < iOS;非当地语言用户↑需关注供给;T2 城市占比↑拖累大盘。SA 地区需额外关注 Keemart 电商流量占比上升对大盘访购率的稀释效应(Keemart 访购率约 2~3%,远低于 Keeta 外卖的 30%+)。若供需路径已定位为 DAU 变化驱动,用户结构通常是被动结果。

**⚠️ Path 3 强制要求**:用户结构分析**必须按§7.0双因素拆解计算贡献度**,禁止仅呈现各分层访购率变化幅度。输出格式(按|总贡献|降序,cxr_fetch.py 已自动计算):

| 分层 | DAU(基期→本期) | 访购率(基期→本期) | 趋势贡献(访购率自身变化贡献) | 结构贡献(DAU占比变化贡献) | **总贡献** |
|---|---|---|---|---|---|
| XXX | Xw→Xw(+/-N%) | X%→X%(+/-Xpp) | +/-Xpp | +/-Xpp | **+/-Xpp** |

结论必须给出:趋势贡献合计 vs 结构贡献合计哪个更大 → "效率驱动"还是"结构优化(被动)"。

### Path 4:流量分发

#### 主分析(首轮必做,数据集 60051984 + 62059514)

**止于模块级**:定位"哪个模块有问题"(曝光结构变化 vs 模块自身效率变化),不需进入模块内部。

**⚠️ 必须按§7.0贡献度归因**:各模块对大盘访购率的影响必须用双因素拆解(趋势=访购率变化×基期曝光占比,结构=曝光占比变化×(模块访购率-大盘访购率)),按|总贡献度|排序定位主因。禁止仅凭"访购率变化最大"或"曝光UV变化最大"归因。

典型场景:低访购率模块占比上升(结构负贡献);模块 CTR↑但访购率↓(落地承接差);AB实验/新模块上线挤占曝光。

搜索专属(数据集 60052643/60052634):搜索框 CTR 下降 = 分发问题;搜索内部转化下降 = 供给/算法问题。HK 用 `search_no_result_rate_new` 字段。

| 广告规模 | 62059760 | Adload(各模块广告规模),维度:`ad_module_name` - **已迁入 keeta-data-ad-analysis** |
| 广告实验大盘累计 | 62059762 | 广告实验增量(大盘整体,实验组 vs 对照组) - **已迁入 keeta-data-ad-analysis** |
| 广告实验模块累计 | 62059764 | 广告实验增量(按`ad_module_name`拆分) - **已迁入 keeta-data-ad-analysis** |
| 搜索结果分析 | 60052643 | 搜索效率、结果质量 |
| 离线用增大盘 | 60051108 | 进店细拆、提单页优惠质量、支付后取消、加购漏斗、商家/用户维度下钻;**Path 2 价格信号**(实付配送费/AOV/单均补贴)|
| 商家入口流量分析-资源位 | 60048404 | 商家入口进店UV、商家入口支付UV、入口CVR、页面×资源位来源分析 |
| 服务体验数据 | 62052307 | 取消场景分类 |

---

## 五-B、用户访购率模式差异

当用户选择"用户访购率"口径时,以下取数逻辑与"设备访购率"不同:

| 分析环节 | 设备访购率(默认) | 用户访购率 |
|---|---|---|
| 大盘异动 | 60051984 `davg_visit_txn_ratio` | 60051108 `davg_visit2ord_rate` |
| Path 1A 渠道拆分 | 62055197 `app_uv` + `fin_ord_uv` | 62055197 `visit_usr_num` + `fin_usr_num`(逐天查求日均) |
| Path 3 用户结构-维度 | 60051984 按 `user_finord_type_name` | 60051108 按 `upto_yesterday_user_layers_name`、`lifecycle_global_name`、`os`、`device_language_code` |
| Path 3 用户结构-指标 | `davg_visit_txn_ratio` + `davg_dau` | `davg_visit2ord_rate` + `davg_visit_usr_num` |
| Path 3 SA Keemart 拆分 | Hive `aggr_flow_union_visit_biz_type_d` 按 `biz_type` | Hive `aggr_flow_usr_visit_biz_type_d` 按 `biz_type` |
| Path 3 SA Keemart 指标 | `davg_dau` + `davg_visit_txn_ratio` + `txn_union_id_cnt` | `davg_visit_usr_num` + `davg_visit2ord_rate` + `txn_user_id_cnt` |
| Path 5 漏斗 | 60051984 设备口径漏斗 | 60051108 user_id 口径漏斗 |
| Path 5 漏斗指标 | `davg_device_visit_shop_cvr`等 | `davg_visit_shop_cvr`、`davg_visit_shop_submit_page_cvr`、`davg_submit_page_submit_cvr`、`davg_submit_pay_cvr`、`davg_pay_fin_cvr` |

其余 Path(Path 2 补贴、Path 4 模块分发、搜索质量、下钻等)两种口径一致,无差异。

---

## 六、取数实现约束

- **并行取数是全局默认行为**:各查询无数据依赖时必须并行,禁止默认串行。
- 默认先查询完整分析区间;仅在重试后仍失败时,才降级为逐天查询。
- 若某一条 Path 查询失败,保留其他路径结果,标注缺失路径及对结论强度的影响。

### 取数命令

> i️ 以下命令由子 agent 执行(见 §六-B 执行架构),主 agent 不直接调用。

```bash
# 设备访购率(默认,向后兼容)
python3 ~/.openclaw/skills/keeta-data-v2r-analysis/scripts/cxr_fetch.py \
  --region QA --date 20260419 --compare 20260412 --out /tmp/cxr_QA.json --markdown

# 用户访购率
python3 ~/.openclaw/skills/keeta-data-v2r-analysis/scripts/cxr_fetch.py \
  --region QA --date 20260419 --compare 20260412 --mode user --out /tmp/cxr_QA_user.json --markdown
```

天气补充:
```bash
python3 ~/.openclaw/skills/keeta-external-context/scripts/weather.py <REGION> <本期日>
python3 ~/.openclaw/skills/keeta-external-context/scripts/weather.py <REGION> <对比期日>
```

---

## 六-B、执行架构(归因-格式化物理拆分)

### 架构原则

首次完整归因必须在 isolated 子 agent 中执行,主 agent 不接触原始数据。后续下钻/追问由主 agent 直接处理(数据量小,规则已在 context)。

```
┌─ 首次完整归因 ─────────────────────────────────────────────┐
│ 主 agent(意图识别 + spawn isolated 子 agent + 格式化输出)  │
│   → 子 agent(读 attribution_core + 取数 + 归因 → 结论文本)│
│   → 主 agent(读结论 + 按 templates/segment.md 模板格式化输出)              │
└──────────────────────────────────────────────────────────────┘

┌─ 后续下钻/追问 ────────────────────────────────────────────┐
│ 主 agent 直接 exec 单 path 取数 → 读少量数据 → 推理输出    │
│ (SKILL.md 已在 context,单 path 数据量小,无 bias 风险)   │
└──────────────────────────────────────────────────────────────┘
```

### 场景判定

| 场景 | 执行者 | 原因 |
|------|--------|------|
| 首次完整归因(14路径全量) | isolated 子 agent | 全量数据大,防 context 污染导致格式失控 |
| 下钻(展开某 path 细节) | 主 agent 直接做 | 单 path 数据量小(几行指标),规则已在 context |
| 换口径/换日期重跑 | isolated 子 agent | 等价于新一次完整归因 |
| 追问结论细节(不需新数据) | 主 agent 直接答 | 结论已在 context,无需取数 |

### 阶段 1:isolated 子 agent 取数 + 归因(仅首次完整归因)

**子 agent 负责取数和归因的全部工作**:

1. **读规则**:读取 `attribution_core.md` + region_notes/{region}.md
2. **取数**:exec `cxr_fetch.py --slim`,数据写入 `/tmp/cxr_{region}_{mode}.json`
3. **归因**:读取数据文件 → 执行完整归因分析

- 读取归因核心规则 `~/.openclaw/skills/keeta-data-v2r-analysis/rules/attribution_core.md` + region_notes/{region}.md
- exec 取数命令(见 attribution_core.md §三-B)
- 读取 `/tmp/cxr_{region}_{mode}.json`
- 按 §四~§七 执行完整归因分析(含交叉验证、贡献度、证据链)
- 执行 skill_tracker start/end
- **输出格式**:只输出归因结论文本(自然段落),包含以下字段:
  - `anomaly_direction`: up/down
  - `funnel_primary_leak`: 主漏损环节 + 贡献 pp
  - `cross_validation_summary`: 哪几条 Path 同向/反向
  - `suggested_situation`: A/B/C 判定
  - `key_signals`: 需要特别关注的信号(如置休率+4.51pp)
  - `main_cause`: 1-3 句自然语言归因结论
  - `secondary_factors`: 次要因素
  - `uncertainties`: 不确定性
- **不输出** Segment 1/2/3 模板格式(格式化由主 agent 完成)

### 阶段 2:主 agent 格式化(仅首次完整归因)

**唯一职责**:读子 agent 的结论文本 → 按 `templates/segment.md` 模板逐段填入 → 输出最终报告

- **读取 `templates/segment.md`（强制前置，不可跳过）**：收到子 agent 结论后必须先 read 模板，禁止凭记忆直接输出
- **回读 SKILL.md §7.1 主因排序原则**：归因段必须逐条对照 §7.1 判定情形和写法，不得凭子 agent 的 suggested_situation 直接采用
- 读取子 agent 的归因结论文本
- 按模板 Segment 1 逐段填入(访购率、归因、外部环境、需求/渠道、补贴、供给/运力、用户结构、流量分发、漏斗、展开提示)
- 确保每句话只讲一个要点,同一要点下可罗列多个相关指标,无异动路径写"查了但无异动"
- 输出最终格式化报告

### 阶段 3:主 agent 下钻/追问(后续交互)

当用户要求展开某个 path 或下钻时,主 agent 直接处理:

1. exec `cxr_fetch.py --drilldown {mode}` 取单 path 补充数据
2. 读取结果(单 path 数据通常 <20 行,不触发 completion bias)
3. 结合已在 context 的 SKILL.md 规则 + 首次归因结论,给出下钻分析

### ⛔ 主 agent 禁止事项

- 禁止 read/cat **首次全量**取数结果文件(`/tmp/cxr_*.json`)--这是触发 completion bias 的根因
- 禁止在未 spawn 子 agent 的情况下自行给出**首次完整**归因结论
- 禁止省略模板段落或输出自由格式总结
- 允许:读取下钻结果文件(单 path,数据量小)
- 允许:用户明确要求"看原始数据"时可 read 文件并展示

### ⛔ 子 agent 禁止事项

- 禁止直接输出 Segment 1 模板格式(只输出归因结论,格式化由主 agent 完成)
- 禁止省略任何 Path 的分析(即使无异动也要说明)
- 禁止只输出"稳定"/"无异动"等一句话带过,必须给出具体数值变化

### 子 agent spawn 参数模板

```
你是 Keeta 访购率归因分析师。

参数: region={region}, date={date}, compare={compare}, mode={mode}

⚠️ 时间预算:约 2-3min 取数 + 2-3min 归因,建议设置 timeout ≥ 8min

1. 读取 ~/.openclaw/skills/keeta-data-v2r-analysis/rules/attribution_core.md
2. 若存在 region_notes/{region}.md,读取地区专属规则
3. 执行 skill_tracker start
4. 执行取数命令(见 attribution_core.md §三-B),将结果写入 /tmp/cxr_{region}_{mode}.json
5. 读取 /tmp/cxr_{region}_{mode}.json
6. 按 attribution_core.md 中的规则执行完整归因分析。⛔ 归因判定必须严格按 attribution_core.md §7.1 主因排序原则执行：先按两层框架定位，再按情形 A/B/C 判定。多路径解释力接近时并列，不得强行降级。输出以下字段的自然语言结论(不要按 Segment 模板格式,只给分析结论):
   - anomaly_direction: up/down
   - funnel_primary_leak: 主漏损环节+贡献
   - cross_validation_summary: 同向/反向 Path
   - suggested_situation: A/B/C（必须按§7.1判定）
   - key_signals: 异常信号清单
   - main_cause: 1-3句归因结论（按§7.1情形写法）
   - secondary_factors: 次要因素
   - uncertainties: 不确定性
7. 执行 skill_tracker end
```

---

## 七、贡献度与结论强弱规则

### 7.0 ⚠️ 维度拆分必须用贡献度归因(全局强制)

**核心规则**:凡是按维度拆分分析率值型指标(访购率、转化率、CVR 等),**必须计算各维值的规模加权贡献度**,禁止仅凭"哪个维值变化 pp 最大"就判定主因。

**双因素拆解法(比值型指标通用公式)**:

设指标 X = B/C,维值 i 的指标值 x^i,分母 c^i(如进店UV、曝光UV),总分母 C = Σc^i:
```text
趋势贡献值(效率): t_x^i = (x1_i - x0_i) × (c0_i / C_0)
结构贡献值:       s_x^i = (c1_i/C_1 - c0_i/C_0) × (x1_i - X_0)
总贡献值:         d_x^i = t_x^i + s_x^i
验证:            Σ d_x^i = X_1 - X_0(误差 < 0.05pp)
```

**适用场景与分母定义**:

| 分析场景 | 指标 X | 维度 | 分母 c^i | 总分母 C |
|---|---|---|---|---|
| Path 1 渠道访购率 | 渠道访购率 | primary_access_channel_code + secondary_visit_channel_code | 渠道DAU(62055197.app_uv) | 总DAU |
| Path 2 补贴结构 | 分层补贴率 | lifecycle/sales_type/price_range | 各分层实付交易额(actual_pay_no_tip_gmv) | 总实付交易额 |
| Path 3 用户结构 | 分层访购率 | 新老客/设备/语言 | 分层DAU | 总DAU |
| Path 4 模块访购率 | 模块访购率 | first_ad_position_name | 模块曝光UV | 总DAU* |
| Path 5 下钻-入口CVR | 入口CVR | 页面×资源位 | 入口进店UV | 总进店UV |
| Path 5 下钻-商家维度 | 环节转化率 | shop_type_name | 商家类型进店UV | 总进店UV |
| Path 5 下钻-用户维度 | 环节转化率 | uuid_layers_3_name | 用户分层进店UV | 总进店UV |

*注:模块之间用户重叠(一人看多个模块),模块曝光UV之和 > DAU。公式仍以 DAU 为总分母 C(因为访购率 = 交易UV/DAU),模块贡献度合计可能 ≠ 大盘访购率变化(存在交互项误差),但排序仍有效。当模块贡献度合计与大盘偏差 > 0.3pp 时,需标注⚠️。

**Path 2 补贴结构贡献度**:补贴率(实付补贴率 = 补贴金额/实付交易额)双因素拆解的分母为**各分层实付交易额**(`actual_pay_no_tip_gmv`),脚本已自动查询并计算加权贡献度。

⚠️ **Path 1 渠道贡献度口径注意**:62055197(渠道数据集)与 60051984(大盘数据集)DAU 口径不同(渠道DAU之和可能 > 大盘DAU),贡献度合计与大盘偏差可能 > 0.05pp。偏差 > 0.2pp 时需在报告中标注⚠️并说明口径差异。

**输出要求**:
1. 报告中凡涉及维度拆分的结论,必须按贡献度(|d_x^i|)排序,而非按 pp 变化排序
2. 每个维值至少给出:`趋势贡献([指标名]自身提升贡献)` + `结构贡献([分母名]占比变化贡献)`,括号内替换为实际指标/分母名称(如"访购率自身提升贡献"/"DAU占比变化贡献")
3. 判定主因时,"贡献度最大"优先于"变化幅度最大"
4. 贡献度合计需与实际整体变化做一致性校验

**漏斗环节贡献度**(不适用双因素,单独公式):
```text
某环节贡献度 ≈ 该环节转化率变化(pp) × 基期其他所有环节转化率之积
```

> `cxr_fetch.py` 已自动计算所有贡献度,结果在 JSON 的 `contributions` 字段(`contribution_pp` / `structure_contribution_pp` / `efficiency_contribution_pp`),无需重新计算。

> **前置判断 Gate(强制执行)**:JSON 输出中的 `gate_checks` 字段包含脚本已计算好的前置判断,**分析前必须先读取,按以下规则严格执行**:
>
> | 字段 | `pass=true` | `pass=false` |
> |---|---|---|
> | `promo_zone_gate` | 正常分析 `drilldown_s2o_promo_pv` 数据 | **直接将 `analysis_conclusion` 字段文本原文输出,不得自行展开任何优惠专区指标** |
> | `submit_page_discount_amt_gate` | 正常解读提单页优惠金额变化 | **直接将 `analysis_conclusion` 字段文本原文输出,不得解读该指标变化比例** |
>
> `pass=false` 时禁止基于原始数据做任何推断,即使看到数值有明显变化。

### 7.1 主因排序原则

**核心原则:证据强度第一,贡献度辅助定量。** 漏斗贡献度和各 Path 的单点指标都是"定位工具"(告诉你变化集中在哪个环节/方向),不是归因结论。归因必须回答"它为什么变化",答案来自交叉验证与下钻,而非任何单一定位信号本身。

#### 两层归因决策框架(强制)

归因分两层,**定位**与**下钻**解耦,下钻对所有 Path 通用(不是漏斗专属):

**第一层 · 定位(对应 Segment 1)**
1. **Path 0 外部环境前置闸门**:先排查天气/节假日/赛事/竞对/政策/基础设施等外部事件。
   - 有显著外部事件且方向吻合 → 外部环境为主因或重要背景,内部归因须在"考虑/剔除外部影响"前提下进行
   - 无显著外部异常 → 进入下一步(无异常即省略,不在 Segment 1 展开)
2. **Path 1~5 + 漏斗交叉验证**:找出证据最强的候选方向(可能是补贴/用户结构/供给/模块/漏斗环节中的任意一个)。
   - 某方向指标与访购率同向、断点吻合、机制清晰 → 锁定为候选主因
   - 仅漏斗某环节贡献显著、而 Path 0~5 外部路径均无同向证据 → 锁定该环节为候选方向(根因待下钻)
   - 各方向均无清晰信号 → 进入情形 C(诚实保留)

**第二层 · 下钻(对应 Segment 2,按需触发)**
对第一层锁定的**任意方向**继续拆解到可行动的子原因(补贴→营销类型/生命周期/客单价;流量→渠道/资源位;供给→品类/商家分层;漏斗环节→入口/承接/技术指标)。
- 拆到可解释、可行动的子原因 → 升级为情形 A/B,明确主因
- 拆解后仍无清晰原因 → 情形 C,诚实保留

#### 主因判定(按"是否已下钻到可行动根因",不按贡献度大小)

- **情形 A(已定位 + 已下钻到根因)**:因果链闭合、方向一致、断点吻合 → **强证据,明确主因**,写"[根因指标变化] → [作用机制] → [影响的环节/大盘]"
- **情形 B(已锁定方向,但尚未下钻到根因)**:Segment 1 阶段只锁定到"哪个方向"(含漏斗环节自身),尚无内部子原因 → 诚实输出"主因方向锁定在 XX,根因待下钻",**触发第二层下钻**,不在 Segment 1 强行写死成因
- **情形 C(下钻后仍无清晰根因 / 证据不足)**:诚实输出"主漏损/主波动在 XX,但当前数据未定位到明确根因,需进一步排查 [方向]",**禁止为凑结论强行归因到某条 Path**
- 路径指标方向与访购率相反 → **反向信号,作为排除依据**;路径无显著变化 → **排除项**

**贡献度的正确用法**:
- 可量化时(如 STP、漏斗、入口):写"贡献约 Xpp",辅助说明量级
- 不可量化时(如供给爆单率、ETA、外部事件):只写方向判断和核心数字
- **禁止**:把"定位/现象"当归因结论。无论根因来自外部 Path、内部某 Path 还是漏斗环节自身,主因都必须回答"为什么变化",不得停留在"XX→XX 转化率下降了""补贴率下降了"这类现象层。
- 漏斗环节**可以**作为主因,前提是已下钻到该环节内部的具体原因(承接/入口/技术指标等),且 Path 0~5 交叉验证已排除外部及其他路径驱动。
- **何时锁定主因 / 何时标注待下钻**:Segment 1 阶段,只有某方向的根因凭常规取数即可闭合(如补贴率、渠道结构、供给指标)才写为确定主因(情形 A);若锁定的是需要细拆才能解释的方向(如漏斗环节内部、某模块内部),则按情形 B 标注"待下钻"。

不得仅依据某一指标变化最大(含漏斗贡献度最大)就认定主因;多路径解释力接近时可并列为"共同驱动"。**Segment 1 仅执行第一层(定位),不执行第二层下钻;下钻是用户回复「展开」或指定路径后的独立动作(Segment 2)。**

> **与§四取数顺序的关系**:§四的 "Path 0→5 不得跳步" 是**取数顺序**(所有 Path 数据都要拉齐);本节是**归因研判顺序**(按证据强度筛选主因)。两者不冲突:先全量取数,再按证据强度判断谁是主因。

**结论强度**:强结论(证据充分,可明确主因)/ 弱结论(方向明确但不能定量闭环)/ 暂无强结论(只有观察)。禁止为了报告好看把弱结论写成强结论。

**每条 Path 证据链必须覆盖四类**:支持证据(指标方向+断点)/ 反向信号(哪些数据不支持)/ 排除项(为何排除为主因)/ 不确定性(尚未闭环的环节)。

---

## 八、查询失败 Fallback 规则

| 触发条件 | Fallback 处理 |
|---|---|
| 区间查询超时/失败 | 降级为逐天查询;结论注明"逐天汇总口径" |
| 60048561 查询失败 | 改用 60051984 中的 `actual_disc_ratio` / `actual_mt_charge_amt_ratio`;仍失败则填 N/A |
| 60051984 漏斗/分组指标返回空 | 改用 60051108 查同类指标 |
| 62059514 超时 | 重试 3 次 → 逐天聚合 → 仍失败则标注路径数据不足 |
| 字段名报错 | 用 `origin_query measures --dataset <ID>` 搜索近义字段 |

---

## 九、基期推算规则

| 本期时间类型 | 识别方式 | 基期推算 |
|---|---|---|
| 7天整周 | 时间跨度 = 7天 | 前7天(WoW) |
| 单天 | 开始日 = 结束日 | **必须先询问用户**:DoD / WoW / 指定日期 |
| 月份 | 用户明确输入"X月" | 上一个自然月(MoM) |
| 其他多天(N天,N≠7) | 时间跨度 = N天 | 往前推 N 天(PoP) |

### ⛔ 参数构造规则(强制)

**`--date` / `--compare` 跨度 ≡ 用户给出的跨度,严禁缩减。**

- 多天区间 → `--date YYYYMMDD~YYYYMMDD`,`--compare` 同跨度紧贴前推
- 单天 → `--date YYYYMMDD`,对比期按§九基期推算表处理(必须先问用户)

---

## 十、交付模板(Segment)

> ⚠️ 模板内容已拆分至 `templates/segment.md`,主 agent 在阶段 2 格式化输出时必须先 `read` 该文件。

### 阶段 2 引用路径

- **Segment 1/2/3 格式模板**: `~/.openclaw/skills/keeta-data-v2r-analysis/templates/segment.md`
- **§10.05 归因决策步骤**: 在 `templates/segment.md` 中
- **§10.1~10.4 输出格式**: 在 `templates/segment.md` 中

---

## 十一、提交前自检 Checklist

### 通用
- [ ] **输出使用 `templates/segment.md` Segment 模板,非自由格式**
- [ ] **已读 `baseline_health` 且已处理**(§3.0):verdict=`baseline_anomaly`/`current_anomaly` 时未直接拿原环比差归因;baseline_anomaly 已按 suggested_baseline 换基期重跑或在结论标注"含均值回归";cyclicality_note 非空已扣除周期性
- [ ] **已读 `anomaly_day_check` 且已处理**(§Path 0 附加):verdict=`anomaly_day_dominated` 时访购率段已使用"稀释/放大"模板表述异常日影响,以剔除后Δ为归因主线,尾部已追加下钻引导语
- [ ] 标题为「[地区] [设备/用户]访购率异动分析 · [本期周期]」
- [ ] 注明本期/对比期具体日期及对比类型
- [ ] **口径已由用户显式确认**(设备/用户),非 Agent 自行推断
- [ ] Segment 1 覆盖全部槽位(访购率、归因、外部环境、需求/渠道、补贴、供给/运力、用户结构、流量分发、漏斗、展开提示),各段不得省略有数据的段落
- [ ] 率值类只写 pp,无"相对+X%"
- [ ] 漏斗贡献度合计与大盘访购率变化误差 < ±0.1pp(否则标注⚠️一致性警告)
- [ ] **维度拆分归因均使用§7.0贡献度**:模块/入口/商家/用户等所有按维度拆分的结论,必须基于双因素拆解的贡献度排序,非变化幅度排序;未仅凭"漏斗贡献度最大/某指标变化最大"就直接定为主因,已做 Path 0~5 交叉验证
- [ ] **Path 3 用户结构必须输出贡献度表格**(趋势贡献+结构贡献+合计,按|合计|排序),并给出"效率驱动 vs 结构优化"判断
- [ ] 证据链含4类(支持/反证/排除/不确定性)
- [ ] **主因不是现象描述**:主因回答了"为何变化"(根因→机制→环节),而非停在"XX转化率下降/补贴率下降";若主因为漏斗环节自身,已有环节内部下钻证据,否则按情形 B 标注"待下钻"(§7.1)
- [ ] **归因段若主语为漏斗环节名称(如"进店→提单页""提单页→提单"等)**:必须确认已完成 `templates/segment.md` §10.05 交叉验证且 Path 0~5 均无同向根因,否则改写为外部根因→机制→环节
- [ ] 模块访购率(`davg_cxr_uv`)为字符串如"12.34%",直接读取不乘100
- [ ] **用户可见输出统一使用"访购率"表述**,已确认无英文缩写
- [ ] UV 类字段可能含逗号(如"315,825"),去除后再转数值

### DoD 专属
- [ ] 星期几不同时已做 weekday check
- [ ] 检查当天是否存在突发天气/活动上下线/系统异常
- [ ] 检查补贴是否有单日配置波动

### 访购率下跌时
- [ ] 检查天气/节假日背景
- [ ] 检查是否 Payweek 结束后自然回落
- [ ] DAU 是否大涨带来低意图流量稀释
- [ ] 营业商家数是否下降
- [ ] 搜索无结果率是否同步上升
- [ ] C补/实付补贴率是否下降
- [ ] 是否存在资源位结构变化

### 访购率上涨时
- [ ] 是否补贴/活动/节律性拉动
- [ ] 哪个模块/客群/环节是主要拉动项
- [ ] 上涨是否可持续

---

## 十二、输入输出约定

**Region**:`SA/AE/HK/QA/KW/BH/BR` **日期**:`YYYYMMDD~YYYYMMDD` **外部环境**:`keeta-external-context` skill

**输出质量底线**:结论用普通人能看懂的语言;数字必须有数据查询来源,禁止用"~""约"等模糊表述。

---

💬 这个回答有帮助吗?回复 👍 或 👎,有具体意见也欢迎告知。

收到反馈后执行:
```bash
# 👍
python "$SKILL_DIR/scripts/skill_tracker.py" feedback --rating 1
# 👎
python "$SKILL_DIR/scripts/skill_tracker.py" feedback --rating -1 --comment "用户反馈内容"
```
